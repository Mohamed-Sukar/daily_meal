diff --git a/lib/features/home/presentation/widgets/spin_wheel_dialog.dart b/lib/features/home/presentation/widgets/spin_wheel_dialog.dart
--- a/lib/features/home/presentation/widgets/spin_wheel_dialog.dart
+++ b/lib/features/home/presentation/widgets/spin_wheel_dialog.dart
@@ -107,26 +107,24 @@ class _SpinWheelDialogState extends State<SpinWheelDialog>
     return Dialog(
       shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(24)),
-      child: Padding(
-        padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24),
-        child: Column(
-          mainAxisSize: MainAxisSize.min,
-          children: [
-            // Title Header
-            Row(
-              mainAxisAlignment: MainAxisAlignment.spaceBetween,
-              children: [
-                Row(
-                  children: [
-                    Icon(Icons.casino, color: theme.colorScheme.primary),
-                    const SizedBox(width: 8),
-                    Text(
-                      'عجلة الحظ 🎡',
-                      style: theme.textTheme.titleLarge?.copyWith(fontWeight: FontWeight.bold),
-                    ),
-                  ],
-                ),
-                IconButton(
-                  icon: const Icon(Icons.close),
-                  onPressed: _isSpinning ? null : () => Navigator.of(context).pop(),
-                ),
-              ],
-            ),
+      child: SingleChildScrollView(
+        child: Padding(
+          padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24),
+          child: Column(
+            mainAxisSize: MainAxisSize.min,
+            children: [
+              // Title Header
+              Row(
+                children: [
+                  Icon(Icons.casino, color: theme.colorScheme.primary),
+                  const SizedBox(width: 8),
+                  Expanded(
+                    child: Text(
+                      'عجلة الحظ 🎡',
+                      style: theme.textTheme.titleLarge?.copyWith(fontWeight: FontWeight.bold),
+                      overflow: TextOverflow.ellipsis,
+                    ),
+                  ),
+                  IconButton(
+                    icon: const Icon(Icons.close),
+                    tooltip: 'إغلاق',
+                    onPressed: _isSpinning ? null : () => Navigator.of(context).pop(),
+                  ),
+                ],
+              ),
@@ -243,3 +241,4 @@ class _SpinWheelDialogState extends State<SpinWheelDialog>
           ],
         ),
       ),
+      ),
     );
   }
diff --git a/lib/features/settings/presentation/settings_screen.dart b/lib/features/settings/presentation/settings_screen.dart
--- a/lib/features/settings/presentation/settings_screen.dart
+++ b/lib/features/settings/presentation/settings_screen.dart
@@ -38,13 +38,15 @@ class SettingsScreen extends ConsumerWidget {
                       Row(
-                        mainAxisAlignment: MainAxisAlignment.spaceBetween,
                         children: [
-                          Text(
-                            'فترة استبعاد الأكلات (Cooldown)',
-                            style: theme.textTheme.titleMedium?.copyWith(
-                              fontWeight: FontWeight.bold,
+                          Expanded(
+                            child: Text(
+                              'فترة استبعاد الأكلات (Cooldown)',
+                              style: theme.textTheme.titleMedium?.copyWith(
+                                fontWeight: FontWeight.bold,
+                              ),
                             ),
                           ),
+                          const SizedBox(width: 8),
                           Container(
                             padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
                             decoration: BoxDecoration(
diff --git a/lib/features/vault/presentation/add_edit_meal_dialog.dart b/lib/features/vault/presentation/add_edit_meal_dialog.dart
--- a/lib/features/vault/presentation/add_edit_meal_dialog.dart
+++ b/lib/features/vault/presentation/add_edit_meal_dialog.dart
@@ -196,10 +196,14 @@ class _AddEditMealDialogState extends ConsumerState<AddEditMealDialog> {
                           DropdownButtonFormField<MealCategory>(
                             key: const Key('meal_form_category_dropdown'),
                             initialValue: _selectedCategory,
+                            isExpanded: true,
                             decoration: const InputDecoration(
                               labelText: 'التصنيف *',
                               border: OutlineInputBorder(),
                               prefixIcon: Icon(Icons.category_outlined),
                             ),
                             items: MealCategory.values.map((cat) {
                               return DropdownMenuItem(
                                 value: cat,
-                                child: Text(cat.labelArabic),
+                                child: Text(
+                                  cat.labelArabic,
+                                  overflow: TextOverflow.ellipsis,
+                                ),
                               );
                             }).toList(),
@@ -222,10 +226,14 @@ class _AddEditMealDialogState extends ConsumerState<AddEditMealDialog> {
                               Expanded(
                                 child: DropdownButtonFormField<ProteinType>(
                                   key: const Key('meal_form_protein_dropdown'),
                                   initialValue: _selectedProtein,
+                                  isExpanded: true,
                                   decoration: const InputDecoration(
                                     labelText: 'نوع البروتين *',
                                     border: OutlineInputBorder(),
                                     prefixIcon: Icon(Icons.egg_alt_outlined),
                                   ),
                                   items: ProteinType.values.map((p) {
                                     return DropdownMenuItem(
                                       value: p,
-                                      child: Text(p.labelArabic),
+                                      child: Text(
+                                        p.labelArabic,
+                                        overflow: TextOverflow.ellipsis,
+                                      ),
                                     );
                                   }).toList(),
@@ -245,10 +253,14 @@ class _AddEditMealDialogState extends ConsumerState<AddEditMealDialog> {
                               Expanded(
                                 child: DropdownButtonFormField<CarbsType>(
                                   key: const Key('meal_form_carbs_dropdown'),
                                   initialValue: _selectedCarbs,
+                                  isExpanded: true,
                                   decoration: const InputDecoration(
                                     labelText: 'نوع النشويات *',
                                     border: OutlineInputBorder(),
                                     prefixIcon: Icon(Icons.bakery_dining_outlined),
                                   ),
                                   items: CarbsType.values.map((c) {
                                     return DropdownMenuItem(
                                       value: c,
-                                      child: Text(c.labelArabic),
+                                      child: Text(
+                                        c.labelArabic,
+                                        overflow: TextOverflow.ellipsis,
+                                      ),
                                     );
                                   }).toList(),
diff --git a/lib/features/vault/presentation/widgets/meal_vault_card.dart b/lib/features/vault/presentation/widgets/meal_vault_card.dart
--- a/lib/features/vault/presentation/widgets/meal_vault_card.dart
+++ b/lib/features/vault/presentation/widgets/meal_vault_card.dart
@@ -240,11 +240,13 @@ class MealVaultCard extends ConsumerWidget {
       child: Row(
         mainAxisSize: MainAxisSize.min,
         children: [
           Icon(icon, size: 12, color: theme.colorScheme.onSurfaceVariant),
           const SizedBox(width: 3),
-          Text(
-            label,
-            style: TextStyle(
-              fontSize: 11,
-              color: theme.colorScheme.onSurfaceVariant,
+          Flexible(
+            child: Text(
+              label,
+              maxLines: 1,
+              overflow: TextOverflow.ellipsis,
+              style: TextStyle(
+                fontSize: 11,
+                color: theme.colorScheme.onSurfaceVariant,
+              ),
             ),
           ),
         ],
